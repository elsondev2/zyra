# Android feature coverage

Implementation and validation are in progress. This is not yet a production-ready release. Tests below use synthetic data and temporary hosts unless explicitly stated.

| Area | Implemented | Remaining release gates |
| --- | --- | --- |
| Native Android | Kotlin/Compose machines, projects, chats, models, workspace, terminal, agents/workflows, questions, dedicated About/licenses; Desktop theme tokens, Bricolage/Hanken fonts and custom controls; native Layoutlib preview fixtures | Interactive accessibility, process-death and device tests |
| Secure LAN access | Opt-in Desktop listener; one-use expiring QR/link; pinned TLS; Keystore device credentials; hashed host tokens; revoke live clients | Actual Android TLS/QR/firewall/network-change tests |
| PC setup | Automatic workspace/network setup, all-project default, per-device project visibility, separate network settings, QR, live connection status and revocation; authenticated re-pair retains device identity and scope | Physical pairing, packaged install and update tests |
| Canonical sessions | Attach/join preserves Desktop authority, plugins and filesystem configuration; current live message/tools/attention snapshots | End-to-end live Desktop/Android projection, full timeline/artifact parity |
| Approval ownership | One canonical decision; stale conflicting client responses rejected | Device approval, cancellation and cross-surface visual checks |
| Questions | Native text, choice, multi-select, confirmation, number, date, file-reference and ranking inputs | Device keyboard/date/file-selection UX; full runtime questionnaire |
| Streaming/recovery | Compact deltas, 50 ms batching with exact sequence ranges, reconnect backoff, current-state recovery and bounded caches | Loss/latency stress, lifecycle/background recovery |
| Durable sends | Local outbox and PC receipt journal; uncertain commands never automatically resent; batched receipt reconciliation | Process-kill and actual Android storage failure tests |
| Server storage | Replay and journal capped by event count and bytes; bounded legacy-journal startup read | Broader load soak and large real-session fixtures |
| Chat catalog | All-machine default and picker, per-PC cursors, Desktop V2 project/activity/title/model/machine cards and V1 option, project artwork, rename/archive/restore; Desktop indexed message search with context and per-device visibility checks | Unread/manual-settled and fork/delete parity; complex unsupported SVG uses brand/folder fallback; real multi-PC interaction |
| Models/settings | Compact model/thinking sheet with connection-scoped catalog cache; PC session configuration drives selected controls; Desktop-derived themes, three font choices, motion and sidebar preferences | Capability-driven settings and complete configuration parity |
| Files/Git review | Shared-root/symlink checks, file paging, rendered Markdown editing and source mode with conflict detection, bounded chunk downloads and Android sharing, same-root file move/rename without overwrite, turn-by-turn review as the default Changes view, Git status/staged/working diff | Physical edit/share/move and review interaction; folder operations, Git actions and complete Desktop review parity |
| Shared terminal | Real Desktop PTY adapter; ANSI screen snapshots; native Termux renderer; input/stop, scroll, copy/paste, create/end/detach; list/detail navigation and two independent panes with capability negotiation and stale-detach protection | On-device keyboard/geometry/accessibility, multi-view interaction, sustained throughput |
| Agents/workflows | Paged run summaries, native definitions/instructions, start/status/stop, agent follow-up/retry/transcript, workflow pause/resume | Runtime action integration, complete transcript paging and workflow argument validation |
| Attachments/media | Native photo picker, inline camera and optional JPEG optimization; resumable upload/download with checksums; durable image drafts; bounded UTF-8 text/source attachments (180 KB each, 1 MiB combined); lazy image history, live previews, zoom and save | Physical camera/picker/share interaction, arbitrary binary document ingestion, full image format/orientation checks and richer artifacts |
| Voice | Source includes native audio-only WebRTC, microphone permission, mute/output controls, typed Voice messages, Desktop canonical signaling, bounded ordered transcript forwarding, authenticated client-command replay protection, immediate capture cleanup and connection-specific PC ownership; themed live transcript bubbles reconcile by saved identities and remove suppressed replays; nine Desktop voices with local samples; bounded missing-transcript audio recovery through the PC, cancelled on normal completion/mute/stop | Real provider/microphone/output, background lifecycle and Bluetooth route tests on a physical phone |
| Profiles and chat memory | PC-provided built-in/local speaking-style choices and the current chat's memory-learning switch; canonical configuration and per-thread memory persistence | Phone interaction and broader memory-management parity |
| Plugins | Native installed list, project/new-chat selection, frozen chat versions and explicit update review; machine-level browsing from app settings, optional current-chat scope and per-device limits; atomic review guard and reconnect-safe drafts; compact version checks and coalesced Desktop change notifications; pinned Store with local artwork, scoped PC downloads, progress, concrete install review and cancellation; installed details, reviewed global enable/disable and saved-version restore through Desktop authority | Actual external download and physical interaction; six SVG Store marks use the generic Plugin icon |
| Notifications/background | Ordinary connections pause after a background grace period; a foreground Voice service retains only its PC and provides End Voice in notifications | Optional background attention/completion notifications and physical lifecycle checks |
| Packaging | Verified debug APK, matching update certificate, ZIP/native-library alignment; source licenses included; pinned Gradle wrapper | Release signing, AAB and PC packaging, installation/update tests |

Verified checkpoints: 23 gateway tests; server approval/join/replay-byte-budget tests; existing agent-server tests; Desktop worker tests including actual journal-gap recovery; real shared Windows PTY smoke test; 19 Android app unit tests and 2 native-terminal unit tests. The image pipeline also has real pinned-TLS upload/download routing and Desktop-to-Node bridge fixtures, including image-only prompts, steering and follow-ups. Later edits require their owning checks again before release. No phone/emulator verification has been claimed.

Design-review APK 0.1.2-dev (version code 3) builds and verifies with 37 Android app unit tests passing. Nine native Layoutlib previews cover Chats at normal/narrow sizes, conversation, controls, Appearance, About, inputs, machine-sheet content and the composer, including enlarged text. These use production components with synthetic data. The actual modal window, gestures, camera, keyboard and installation/update remain phone gates. The gateway has real TLS coverage for pairing/re-pairing, visibility, indexed search routing, transfers and revocation; Desktop main typechecking and the running mobile host were checked. This is a signed debug review artifact, not a certified production release.

Recent session views reveal immediately from a bounded cache. Cold opening can reveal persisted history while attachment prepares; an authoritative history read follows attachment. Regression tests cover late attachment cancellation and machine/session ownership. These checks do not establish latency on every PC or network.

Post-0.1.2 source refinements are not yet included in the immutable review APK: history trimming preserves recovery cursors for known persisted rows, and chat preferences expose speaking styles and a per-chat learning switch. Unknown live/legacy rows remain protected from eviction. Preference tests include actual isolated runtime calls with network blocked, gateway scope rejection, native controller races and a narrow native preview. Physical operation and the next package remain separate gates.

The later Voice presentation checkpoint passes 26 targeted Android controller/presentation/timeline tests, the gateway's generated wire fixture and lease tests, Desktop canonical adapter/presentation tests, and the actual isolated PC SDK bridge. The bridge test proves durable save, attached-client delivery and immediate history equivalence without rewriting the previous strong turn. The index regression verifies this read after an append without a catalog list. Twelve native Layoutlib previews have now built, including inspected Voice controls and live transcript bubbles. These checks use synthetic data; microphone, provider audio and physical use remain unverified.

Voice settings adds an inspected thirteenth native preview at 320dp with enlarged text, eight passing controller tests including selected/default voice signaling, and nine bundled sample files verified against Desktop by SHA-256. Playback and audio focus cleanup require physical Android verification.

Missing-transcript recovery passes 22 targeted native tests covering PCM/WAV conversion, capture bounds, cancellation races, transport and canonical-queue integration. The native WAV fixture is accepted by the actual Desktop validator. Thirteen gateway domain tests and the actual TLS fixture cover chunk/checksum validation, connection ownership, cancellation, abandoned uploads, shared memory limits and exclusion from the durable journal. Desktop transcription/Voice ownership/core tests pass, including request/body cancellation and convergence with late original transcripts; main typechecking passes. These synthetic checks do not prove physical microphone capture or provider recognition quality.

A fourteenth native preview covers recovery states at 320dp/font scale 1.3. Final rendering was inspected after removing copy/selection from placeholder messages; actual transcript text retains normal selection and copying. The complete screenshot build passes.

Background Voice source now separates Activity ownership from the app session and starts a non-sticky microphone/media-playback foreground service before media capture. Only the call's PC remains connected in the background. The ongoing notification has an End Voice action; task removal ends capture and drains before releasing the service. The custom audio-output sheet follows confirmed routes on API31+ and shows pending/error state. Fourteen focused native tests pass (12 controller, 2 session ownership), including service startup gating/cancellation and reopening the same live session. These tests do not run the Android OS service or Bluetooth stack; those remain physical-device gates. The existing 0.1.2 review APK predates this source.

Review APK 0.1.3-dev (version code 4) includes the post-0.1.2 source above. Full native unit suites pass: 84 tests across 22 suites, with zero failures/errors. Fifteen native previews generated, including inspected narrow Voice controls and the audio-output sheet with enlarged text. Android lint passed before the final button/icon refinements; its two audio constant warnings were subsequently moved under their API guard. Final combined unit/preview/APK build passed in 6m54s at one worker. The same debug signing certificate, v2 signature, ZIP alignment and all eight packaged native ELF libraries were verified. The dev desktop was restarted through normal window shutdown; its mobile host runs and the canonical service reports a matching current source revision. Physical call/provider behavior remains unverified; this is a review artifact, not production certification.

The Plugin selection/review source passes eleven focused gateway tests (including real pinned TLS, revocation, privacy and atomic revision checks) and seven native controller/parser tests. Eighteen native previews generated; three new Plugin views were inspected at 320dp with enlarged text. An unchanged scoped version check returns under 140 bytes and preserves loaded pages and selection drafts. This source is newer than review APK 0.1.3; Store/install/rollback and physical interaction remain pending.

The native Plugin Store now routes the pinned Desktop catalog and its existing PC acquisition service. Eighteen focused gateway tests pass, including the real TLS preparation/review/install/cancel/revoke flow with a synthetic service; thirteen native selection/Store tests pass. Desktop main typechecking and the host-owner adapter check pass. Twenty-one previews generated, including inspected narrow Store, download and review screens. Fifty-six bundled publisher logos verify against Desktop; no artwork network fetch is needed. Installed versions and disabled state come from the PC, including after an update. Actual external downloads, phone interaction, global enable/rollback controls and a matching review APK remain separate gates.

Review APK 0.1.4-dev (version code 5) includes all Plugin source checkpoints above, with installed details and reviewed enable/disable/restore through Desktop's authority service. The final serial build passed 103 native tests across 25 suites and generated 24 previews. All 67 gateway tests passed, including pinned-TLS access changes, stale review rejection, Store review and cancellation. Desktop main typechecking and synthetic service-adapter checks passed. The APK retains the prior debug signer; v2 signature, ZIP alignment and all eight native ELF libraries verify. The matching dev host runs and its canonical server revision matches source. The LAN page and complete APK download passed size/hash verification. These are local synthetic/render/build checks; real Plugin download, phone gestures, live providers, accessibility and lifecycle remain unverified. Desktop's OpenAI prewarm timed out during restart and is not treated as a successful provider test.

After the 0.1.4 review package, the source adds coalesced Desktop Plugin change notifications. Notifications contain no catalog data and are subscribed only after authorized Plugin access. Native refresh waits for foreground/idle state, preserves open reviews and discards work for replaced connections. Sixty-nine gateway tests, eighteen focused native tests, Desktop typechecking and actual temporary-registry/service-adapter checks pass. This refinement is not yet included in the immutable review APK; physical cross-device convergence remains unverified.


The current design-review source uses Desktop's Vercel/Paper Light defaults and migrates the retired blue Dark preset. Work summaries group chat actions, older history loads near the scroll boundary, and the slim composer opens a compact model/thinking sheet. New chat selects a sole computer automatically, then opens the chosen project directly. Draft restoration preserves edits made during loading. Plugins now opens from app settings with a computer picker, without requiring a chat attachment; saved chat versions remain available through a secondary scope selector. Chat settings keep model, speaking style and memory visible, with approval/web controls under Advanced.

Verification is component-specific: terminal behavior passed eight focused tests. The completed new-chat/model-picker/machine-Plugin checkpoint passed 18 native tests and 40 rendered previews, including narrow and enlarged-font sheets. The compact Plugin defaults-row follow-up also rendered all 40 previews successfully. The machine Plugin scope passes 26 gateway regression tests and two TLS tests, including access isolation and browsing before chat attachment. These changes are not included in the immutable 0.1.4 review APK. Physical gestures, live providers, lifecycle and the other release gates above remain open.
The Files source now has a header switch for Files/Changes, relative breadcrumbs, automatic directory paging, desktop file artwork, and syntax-colored previews with explicit Edit/Save. Folder scroll positions survive a file preview or Git visit. Reload uses the existing unsaved-edit confirmation. Unicode edits are size-checked without silent truncation; the host preserves UTF-8 byte-order marks and existing line endings and keeps binary files out of the text editor. This component passed 13 native controller/presentation tests, three SVG parser compatibility tests, 13 file/Git gateway tests, and 42 rendered previews. Representative colored icons, narrow headers, and Paper Light code previews were visually inspected. AndroidSVG is vendored with a documented parser compatibility patch; previews and the APK share its implementation. These source changes are not yet in the immutable review APK. Physical interaction, file-draft recovery after process death, and SVG project-logo support remain unverified or pending.

The Desktop inbox checkpoint adds canonical model/TUI/activity metadata and flat V2 cards matching Desktop's hierarchy. Old chat model metadata hydrates in the background with a paced reader and batched cache writes; no full-history transfer is needed. Thirteen metadata/backfill tests, the existing incremental-index regression and real temporary Desktop/phone server synchronization pass. Project artwork now includes desktop overrides, bounded static SVG logos and 39 bundled Simple Icons brand marks with Desktop contrast handling; New Chat also loads artwork for projects without prior chats. Complex SVG documents fall back to their brand/folder icon. Host resolver tests and Desktop main typechecking pass. The complete native suite passes 157 tests across 34 suites, and all 45 previews render; project marks were visually inspected in Vercel and Paper Light. These changes remain newer than the immutable 0.1.4 review APK. Real Electron raster decoding, immediate artwork-change pushes, physical interactions and release packaging remain separate gates.

## Review package: 0.1.5-dev

Version code 6 packages the design-review source described above, including Vercel/Paper Light defaults, Desktop inbox cards and project icons, compact chat settings and composer, machine-level Plugins, terminal list/detail and two panes, Files/Changes navigation and syntax previews, and voice transcript reconciliation. Earlier statements that these source checkpoints were newer than the review APK are superseded by this package.

Optional dictation is enabled in Voice settings. Audio stays in memory and is transcribed through the selected PC's existing ChatGPT connection, then inserted into the current draft without sending. Failed transfers require explicit retry; same-chat reconnection retains the clip, while cancellation or leaving its owner clears it. Eleven dictation tests cover cancellation, retry, microphone retirement and draft preservation. Physical microphone/provider behavior remains unverified.

Work rows now use Desktop action families and intent labels, elapsed work time, structured web evidence, captured file/skill previews and agent/workflow details. Live tool updates retain start arguments, timestamps and row position through canonical commits. Large outputs keep bounded action metadata without a duplicate deferred row. Full Desktop outcome/stat preferences and agent-inspector navigation remain follow-up work.

The final serial Android build passed 176 unit tests across 36 suites, all 50 native previews, lint and APK assembly. Narrow action rows and captured-evidence screens were visually inspected. All 90 gateway tests pass. The immutable APK is 47,171,650 bytes; SHA-256 `0d72bda528df6476788b3ece0812e2d0799aafe4441eba96d63761647ab14b29`. Its signing certificate matches the previous review install, v2 signature and ZIP alignment verify, and all eight native ELF libraries have the required alignment. The dev host was restarted and its canonical runtime revision verified. Mobile access is running with the existing paired device. The LAN page, HEAD/range requests and complete APK download passed size/hash checks.

This is an early review delivery requested by the user, not a production release. Live provider, phone, accessibility and lifecycle checks remain open. Desktop's OpenAI startup prewarm timed out; this is not counted as a successful provider check. Work-projection performance, actual voice RMS, broader startup latency and remaining feature-parity work continue separately.

## Review package: 0.1.6-dev

Version code 7 adds canonical remote-turn boundaries for Desktop, stable bounded history hydration, readable project catalog names, a floating morphing composer with actual audio activity, optional timestamps/work statistics, the project/machine subtitle, terminal long-press actions, compact Files/Changes and Appearance, title regeneration, and authenticated phone-presence indicators. Local project images render within Markdown using scoped, checksum-verified chunk transfers; larger images load explicitly. Rich Markdown parity, local file-link navigation and physical interaction remain open.

The full Android build passes 203 tests across 40 suites; lint reports zero errors and 33 warnings. Native narrow/enlarged-text previews of the changed surfaces were inspected, including an actual decoded image within Markdown. The APK is 47,272,384 bytes with SHA-256 `942c6293638361499d4ddf4a7c2691b59c6206bf932ecf2a404a2dc4728cba23`. The existing review signing identity, v2 signature, ZIP alignment and eight native ELF libraries verify. Full LAN download and range handling were checked. These checks do not establish production readiness or replace phone/provider/lifecycle testing.

Post-0.1.6 source adds a structured Agents/Workflows result page, expandable technical detail, a separate transcript, the shared start sheet with optional arguments/instructions, accepted-send draft clearing and detail-to-list Back navigation. Six focused controller tests and scoped Kotlin/Compose integration pass. A 320dp/font-scale-1.3 result preview was inspected. These refinements require a newer verified APK before review on the phone.

## Review package: 0.1.7-dev

Version code 8 packages the Agents/Workflows refinement above. The final ordinary Android build passes 207 tests across 40 suites, APK assembly, and lint (zero errors, 33 warnings). A compiler metadata-limit failure was diagnosed and resolved by raising the bounded metadata allocation to 384 MiB; builds remain offline with one worker/CPU and a free-RAM cutoff. The verified idle development window was gracefully paused for the build and restored afterward.

The immutable APK is 47,415,943 bytes; SHA-256 `311d4ce8fcc062fe89ec147d982d2dabe99c8727d6538c9a7ff3d61981c668ab`. Its certificate matches the previous review install, and v2 signing, ZIP alignment, and all eight native libraries verify. The restored PC host retains its paired device and matches the canonical runtime revision. The LAN download page, full file hash, HEAD, byte ranges, and stale-range fallback were verified. The remaining production gates in the coverage table still apply; this review package does not claim full Desktop Markdown parity or physical-device validation.

## Previous review package: 0.1.8-dev

Version code 9 adds project file links in chat Markdown. Relative paths, Windows paths and local file URLs open in Files; line references open with the target line visible and highlighted. Folder links open directly, and Back from a file returns to its containing folder. Resolution uses the PC's canonical chat roots and the phone's existing access scope; external schemes, remote shares and escaped paths are rejected. File content transfers only after a tap. This uses native text-link interaction rather than sending local paths to a browser.

Verification: all 98 gateway tests pass serially, including paired TLS and scope rejection. All 211 Android tests across 41 suites pass, alongside APK assembly and lint (zero errors, 33 warnings). The actual native 320dp line-link preview was inspected. The full guarded build completed in 9m54s and restored the development window; no Java workers remained afterward.

The APK is 47,415,943 bytes, SHA-256 `b89a29927945150dbebe0ba4b77bbbf84bd226a3e3c3c395be685c9c62588ceb`. Its existing review certificate, v2 signature, ZIP/native-library alignment and new link class in DEX were verified. Full LAN download, HEAD/range requests and stale-range fallback passed. Rich Markdown parity beyond this chat-file-link path, physical phone/provider behavior, accessibility, lifecycle and the other coverage-table gates remain unfinished. This remains a review APK.

## Review package: 0.1.9-dev

Version code 10 adds the immediate New Chat draft screen with computer/project selectors, shared Markdown in user and assistant messages, editable rendered Markdown files, contextual title actions, and canonical turn-by-turn Changes as the default with Git as an alternative. History hydration now fills a bounded recent conversation independently of the viewport, including sparse cached histories; older history continues loading on demand. The composer has aligned 44dp controls and a floating fade, dictation uses finer smoothed audio bars, page transitions remain horizontal, media uses consistent left-aligned thumbnails, and work durations are enabled by default.

Markdown now includes GFM tasks/autolinks/tables, footnotes, section spacing, native anchors, safe HTML/details, code controls and bounded offline Mermaid rendering using the same 11.17.2 engine as Desktop. Rendered editing preserves untouched source and supports paragraphs, headings, list bodies/tasks, tables and code. Complex HTML, diagram source and nested image/link structures remain available through Source; this is not complete equivalence with every Desktop renderer feature. Remote images load only after a tap, using isolated HTTPS requests and bounded caching. Workspace links/images use their current file directory. Hidden nested projects and aliases are excluded from file and Git access without changing terminal authority.

Verification: 273 Android tests pass across 50 suites, APK assembly passes, and lint reports zero errors / 34 warnings. All 110 gateway tests and the Desktop main TypeScript check pass. Sixteen native Layoutlib screens from the integrated app passed and were inspected, including rendered editing, Markdown messages, input modes, new conversation, agent output, turn review, themed code, linked-line positioning and native diagram painting. Real Mermaid flowchart, sequence, gantt and dark-theme output passed the SVG policy with no external requests in the engine harness. These checks use synthetic fixtures; physical keyboard/selection/scroll/voice, live provider and Android WebView lifecycle checks remain device gates.

The signed APK is 51,136,169 bytes (48.8 MiB), SHA-256 `f19ac483d3e041cd1ad39d58e3642704c255e3f391d19fd9328bc56843282582`. Existing review signing identity, v2 signature, 16 KiB ZIP/native-library alignment, full LAN download, HEAD/range and stale-range fallback were verified. Install over the existing review app to retain pairing and drafts. This remains a review APK pending on-device acceptance.

## Review package: 0.1.10-dev

Version code 11 includes coherent bounded long-thread context, deferred mobile tool-output bodies, stable upward paging, work-duration and optional action-count controls, compact question responses, stepped thinking selection, and turn-list/detail/file review navigation. Settings now groups appearance, voice, machines, Limits and Storage. File viewing adds zoomable project images, native save/share, scoped move/rename, adaptive transfers and verified resumable downloads where content hashes are available. Sheet body scrolling no longer drives dismissal, and composer contraction follows the keyboard animation target.

The final Android build passes 299 tests across 55 suites and lint with zero errors and 35 warnings. All 125 gateway tests and the Desktop main typecheck pass. All 21 native previews render successfully; changed work/question, composer, thinking, review, settings, Markdown and file-source surfaces were visually inspected. The immutable APK is 51,386,100 bytes; SHA-256 `f9ad903dfd4e4b997a435835188eb51775c7c8ad57e1e3e2a3961de0c0762ace`. Existing review signing identity, v2 signature, ZIP alignment and all eight native ELF libraries verify. LAN page, HEAD, ranges, stale-range fallback and a full download hash were checked. The Desktop window and paired TLS listener are restored, with the current canonical runtime revision. Physical long-thread scrolling, IME dismissal, sheet gestures, file chooser/share, voice and lifecycle acceptance remain open. Binary downloads without content hashes restart after interruption; file moves require a writable authorized root, an existing destination directory and hard-link support.

## Review package: 0.1.11-dev

Version code 12 groups consecutive mixed actions using Desktop batch intent, moves thread management into Thread details, and distinguishes turn state from neutral turn/file counts. Files search filters the current folder before pagination and debounces requests; hidden-project and real-path checks remain authoritative. Conversation filters use nullable change/work metadata, preserving unknown state until bounded background hydration completes.

Paired startup routes directly to Chats. Explicit navigation direction drives horizontal forward/back transitions. Recent history seeks up to three user-turn boundaries within existing bounded transfer/cache ceilings; paging tracks row keys and pixel offsets, and only deliberate movement toward the end resumes following. Integrated packaging is verified; physical gesture acceptance remains open.

Attachments add a lifecycle-owned inline CameraX panel, system photo selection and supported UTF-8 text/code/data files. Image thumbnails use fixed 4:3 tiles while opening the original. Text attachments are restricted to 180 KB each and 1 MB combined; PDFs, archives and arbitrary binaries are not supported in prompts. Photo optimization is a preference under This phone in Settings. Camera permission, capture, keyboard transitions and background behavior remain device checks.


Usage now has selectable 30-day UTC token/cost bars and proportional model comparisons, with machine and harness selectors. Limits uses theme-aware remaining-quota rings with reset labels, accessible values and reduced motion. The Usage index reads bounded local numeric usage from Zyra, Codex, Claude and OpenCode; project visibility is enforced before grouping. Daily, model and summary totals share deduplication and pricing rules. Partial indexing, unavailable sources and unknown prices stay explicit; API estimates are not subscription charges. Ambiguous Codex fork ledgers remain excluded. Live provider-account values still need acceptance on the paired phone.

Verification: 327 Android tests across 63 suites, 149 gateway tests, Desktop main TypeScript, APK assembly, lint (zero errors, 59 warnings), and all 28 native previews passed. Usage and quota charts were visually inspected at narrow widths and larger fonts. A final singular-label correction was reassembled and its native quota preview rechecked. Heavy checks ran serially; the full build kept at least 1,055,800 KiB free RAM and lint kept 991,732 KiB. No Java build worker remains.

The signed review APK is 56,336,835 bytes (53.7 MiB), SHA-256 `a5551ea4edf4d1c3fabc2ab0138641c31ee9f72c7178d6941a076a35341755d4`. Existing signing identity, version code 12, v2 signature, 16 KiB ZIP/native-library alignment, full LAN download hash, HEAD/range and stale-range fallback all verify. Desktop is restored with the current canonical runtime and existing paired TLS certificate. Install over the current review app to keep pairing and drafts. Physical long-thread paging, keyboard/voice/camera, gestures and live account data remain acceptance gates; this is a review build.

## Review package: 0.1.12-dev

Version code 13 replaces the nested Thread details and Chat settings sheets with a single Chat settings page and a Model & thinking child page. Activity uses compact context, token and cost summaries with an inline breakdown. Speaking style, memory, advanced controls and conversation maintenance remain within that page hierarchy. Returning preserves reading intent, list position and manually expanded completed work.

Usage and limits share one machine selector and one scrolling page, retaining daily token/cost charts and remaining-quota rings. Recorded usage remains harness-scoped and device-project-scoped; provider limits remain account snapshots. Reconnects trigger fresh requests, cached limits survive refresh failures with an age/status label, and cancelled requests cannot overwrite newer results.

Pending agent questions replace the composer content while preserving the draft. Typed answers resume the canonical PC agent exactly once after foreground cleanup; retries, cancellation, Stop and conflicting answers have regressions. Canonical answered-question messages retain the compact question/answer presentation. Thought processes are hidden by default and can be enabled in Appearance. Model controls now use a distinct processor icon.

Desktop live user events retain validated image attachments, including image-only prompts. Android seeds the media cache from owned outgoing originals and displays real pending thumbnails and Markdown. Pending sends appear at the newest end of the rail and reconcile on canonical acknowledgement. Existing machine/project access boundaries and bounded caches remain authoritative.

Verification: all 182 Kotlin main sources were freshly compiled with the exact Gradle-exported Kotlin 2.2.21 / Compose inputs and ordered dependencies. To keep memory bounded, the complete JUnit 4 suite ran separately with the exported test runtime: 343 tests across 66 classes passed. Normal Android packaging and lint passed (zero errors, 60 warnings), as did 158 gateway tests, Desktop main TypeScript, the privacy check and 31 native render fixtures. Source/configuration hashes and every copied class file were checked before signing. The temporary standalone compiler path leaves normal Gradle compiler snapshots untouched.

The signed review APK is 56,496,648 bytes (53.9 MiB), SHA-256 `c9354a125828839d3df1f9a7149fa633f20cc73771e7c8ec8e18338b7c062f72`. Version code 13, the existing review signing identity, APK signature, 16 KiB ZIP/native-library alignment and full LAN download hash verify. HEAD, range and stale-range fallback also pass. Development is restored with the current canonical runtime and the existing paired TLS certificate. Install over the current review app to preserve pairing and drafts. Physical keyboard/voice/camera, long-thread gestures, live account values and cross-device question/image interactions remain phone acceptance checks.

## Review package: 0.1.13-dev

Version code 14 opens Usage & limits on Limits, with a title menu that switches to the separate Usage view. Quotas are grouped by account/model with compact charts; only the selected view fetches data. Chat settings groups context, tokens and recorded cost above preferences and conversation controls. Model controls share a thinking-level gauge.

New mobile chats are persisted before their first history request, including the empty-session case, and retries preserve creation identity. Model catalogs are prefetched per connection and shared across picker opens with bounded stale-while-refresh caching. Active page headers follow measured system insets after cold launch while outgoing pages retain their geometry.

Desktop shows phone live-voice presence from the actual voice lifecycle. Ordinary web links offer Zyra Browser or the default browser on first use, with a per-device preference in Browser settings. Explicit external-browser and authentication actions keep their specified destinations.

Verification: 352 Android tests across 68 classes, 163 gateway tests, staged Desktop main and renderer TypeScript, mobile sync/question-response checks, privacy check, and all 34 native render fixtures passed. Lint reports zero errors and 61 warnings. The new quota and chat-settings fixtures were visually reviewed, including larger text. Ten rapid model-picker opens share one request; cold inset changes have regression coverage. No on-device FPS claim is made.

The signed APK is 54,747,727 bytes, version code 14, SHA-256 `8fcebfbb8b36b8221f42915158aa3f6078503bb59e279a77b37fb5692eb27722`. Existing signing identity, APK signature and 16 KiB alignment verify. LAN page, HEAD, range, stale-range fallback and full download hash pass. Development is restored on the current canonical runtime with the existing paired TLS certificate. Physical-phone new-chat, keyboard/voice/long-thread interactions and native desktop link interaction remain acceptance gates before production release.

